<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--<style name="MainActivityTheme" parent="Theme.AppCompat.Light">
        <item name="android:windowTitleBackgroundStyle">@style/TitleStyle</item>
        <item name="android:windowTitleStyle">@style/windowTitleStyle</item>
        <item name="android:windowTitleSize">50dp</item>
    </style>

    <style name="TitleStyle">
        <item name="android:background">@color/color_green</item>
    </style>

    <style name="windowTitleStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:paddingLeft">25dp</item>
        <item name="android:textSize">20sp</item>
    </style>-->

    <style name="MediaItemList">
        <item name="android:background">@color/mediaListBackground</item>
        <item name="android:paddingBottom">2dp</item>
        <!-- In order to account for optical illusions,
        make the end padding a bit larger so it visually looks the same. -->
        <item name="android:paddingEnd" tools:ignore="NewApi">3dp</item>
        <item name="android:paddingStart" tools:ignore="NewApi">2dp</item>
        <item name="android:paddingTop">2dp</item>
    </style>

    <style name="AppTheme.NoTitle" parent="@style/AppTheme">
        <item name="windowNoTitle">?android:windowNoTitle</item>
    </style>
</resources>
